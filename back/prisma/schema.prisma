generator client {
  provider = "prisma-client"
    output   = "../generated/prisma"

}

datasource db {
  provider = "postgresql"
}

model Source {
  id          Int    @id @default(autoincrement())
  description String
}
